<Window x:Class="laboratornaya1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:laboratornaya1"
        mc:Ignorable="d"
        Title="Калькулятор" Height="400" Width="200">
    <Grid Margin="15">
        <!-- Строки -->
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <!-- Ряды -->
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <!-- Первое число -->
        <TextBlock Text="Первое число:"
                   Grid.Row="0" Grid.Column="2"
                   Margin="5" VerticalAlignment="Center"/>
        <TextBox x:Name="FirstNumberTextBlock"
                   Grid.Row="0" Grid.Column="2"
                   Margin="5" VerticalAlignment="Center"
                   TextAlignment="Right"
                   Text="0"/>
        <!-- Второе число -->
        <TextBlock Text="Второе число:"
           Grid.Row="0" Grid.Column="2"
           Margin="5" VerticalAlignment="Center"/>
        <TextBox x:Name="SecondNumberTextBlock"
           Grid.Row="0" Grid.Column="2"
           Margin="5" VerticalAlignment="Center"
           TextAlignment="Right"
           Text="0"/>
        
        <!-- ТУТ ДОЛЖНЫ БЫТЬ КНОПКИ -->
        
        <!-- Результат -->
        <TextBlock Text="Результат:"
                   Grid.Row="3" Grid.Column="2"
                   Margin="5" VerticalAlignment="Center"
                   FontWeight="Bald"/>
        <TextName x:Name="ResultTextBox"
                  Grid,Row="3" Grid.Column="2"
    </Grid>
</Window>
